# 🎨 Color Theme Picker Guide

## Πώς λειτουργεί ο Color Picker

Προσθέσαμε μια νέα δυνατότητα για να επιλέξετε το χρώμα της εφαρμογής!

### Διαθέσιμα Χρώματα:

1. **Blue** (Γαλάζιο) - #1a73e8 - Το προεπιλεγμένο
2. **Purple** (Μωβ) - #7c3aed
3. **Red** (Κόκκινο) - #dc2626
4. **Green** (Πράσινο) - #16a34a
5. **Orange** (Πορτοκαλί) - #ea580c
6. **Pink** (Ροζ) - #db2777
7. **Cyan** (Κυανό) - #0891b2
8. **Amber** (Κίτρινο) - #b45309

## Πώς να αλλάξετε το χρώμα

1. Πατήστε το κουμπί ⚙️ **Settings** από την κύρια οθόνη
2. Κάντε scroll προς τα κάτω και βρείτε την ενότητα **"Color Theme"**
3. Κάντε κλικ σε ένα από τα χρωματιστά κυκλάκια για να το επιλέξετε
4. Το επιλεγμένο χρώμα θα εμφανίσει ένα ✓ (checkmark)

## Πού εφαρμόζεται το χρώμα

Το επιλεγμένο χρώμα θα αλλάξει το primary color σε όλες τις οθόνες:

- ✅ Τα κουμπιά πλοήγησης (📊 View, 🏗️ Build, 💰 Add $, ⚙️ Settings)
- ✅ Τα titles και headings σε όλες τις οθόνες
- ✅ Τα buttons για εξμός transactions (Deposit, Convert)
- ✅ Τα chip buttons για επιλογή νομίσματος (USD/EUR)
- ✅ Τα icons και τα primary text colors

## Τεχνική υλοποίηση

- **AppContext**: Έχει νέο state `primaryColor` και συνάρτηση `getColors()` που δυναμικά παράγει τα χρώματα με βάση το `darkMode` και το `primaryColor`
- **Color Palettes**: Ορίστηκαν 8 χρώματα στο `colorPalettes` object
- **Dynamic Colors**: Κάθε οθόνη χρησιμοποιεί `getColors()` για να παίρνει τα χρώματα σε real-time

## Παράδειγμα κώδικα

```javascript
// AppContext.js
const [primaryColor, setPrimaryColor] = useState('blue');

const getColors = (colorKey = primaryColor) => {
  const primaryHex = colorPalettes[colorKey] || colorPalettes.blue;

  return darkMode ? {
    // Dark mode colors
    primary: primaryHex, // Αλλάζει με το επιλεγμένο χρώμα
    ...
  } : {
    // Light mode colors
    primary: primaryHex,
    ...
  };
};
```

```javascript
// Σε κάθε οθόνη
const { getColors, primaryColor } = useContext(AppContext);
const dynamicColors = getColors(); // Παίρνει τα τρέχοντα χρώματα

// Χρησιμοποιούμε παντού:
<Text style={[styles.title, { color: dynamicColors.primary }]}>Title</Text>;
```

## Σημειώσεις

- Το χρώμα αποθηκεύεται στο AppContext, όχι στο AsyncStorage (για persistence, μπορείτε να το προσθέσετε)
- Το dark mode συνεργάζεται άψογα με το custom color picker
- Κάθε χρώμα έχει επιλεγεί να φαίνεται καλό τόσο σε light όσο και σε dark mode
